model:
  type: t5
  name: wisenut-nlp-team/KoT5-base

data:
  encoder_max_len: 896
  decoder_max_len: 80
  truncate_tail: true  # 긴 대화는 뒤에서부터 턴을 붙여 enc 길이 이하로 자름
  chunk_overlap: true
  chunk_overlap_tokens: 128

train:
  learning_rate: 3.0e-4   # T5/UL2 계열 권장 범위
  batch_size: 4           # VRAM 여유 시 8까지 상향
  num_train_epochs: 5
  fp16: false
  eval_steps: 500
  save_steps: 500
  gradient_accumulation_steps: 2
  early_stopping_patience: 3
  max_steps: null         # 전체 학습

style_prompt: "질문: 아래 한국어 대화의 핵심 내용을 한두 문장의 간결한 요약으로 정리하시오. 대화 참여자의 역할을 바꾸지 말고, 화자는 누구인지, 행동의 대상이 누구인지도 정확히 유지하라. 지금 하는 일과 앞으로 할 계획을 구분해서 서술하라. 문장은 1~2문장 안으로, 대화 진행 순서(사건 발생 순서)에 맞게 간결하게 적어라."

paths:
  checkpoint_dir: checkpoints/kot5-kakaobrain
  prediction_dir: prediction

wandb:
  use_wandb: false
